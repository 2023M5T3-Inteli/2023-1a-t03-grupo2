import React from 'react'
import { HeroBg, HeroContainer } from './HeroElements'

const HeroSection = () => {
    return (
        <HeroContainer>
            <HeroBg/>
                
        </HeroContainer>

    )
}

export default HeroSection
